apiVersion: v1
kind: Service
metadata:
  name: hello-kubernetes
  namespace: helloworld
spec:
  selector:
    app: helloworld
  ports:
  - port: 8080
    targetPort: 80
    protocol: TCP
    name: http
  - port: 443
    protocol: TCP
    name: https
  type: NodePort